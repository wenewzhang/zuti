#!/bin/bash
set -e

# 创建 zuti 用户（如果不存在）
if ! id -u zuti >/dev/null 2>&1; then
    useradd -r -s /bin/false -M -d /var/lib/zuti zuti 2>/dev/null || true
fi

# 创建必要的目录
mkdir -p /etc/zuti/certs
mkdir -p /var/lib/zuti
mkdir -p /var/log/zuti
mkdir -p /.data/zuti

exit 0
