#!/bin/bash
set -e

# 删除用户（仅在完全卸载时）
if [ "$1" = "purge" ]; then
    if id -u zuti >/dev/null 2>&1; then
        userdel zuti 2>/dev/null || true
    fi
    
    # 删除数据目录
    rm -rf /var/lib/zuti
    rm -rf /var/log/zuti
    
    echo "zuti data has been removed."
fi

exit 0
